export interface WeeklyRevenue {
  week_start: string;
  week_label: string;
  revenue: number;
}

export interface WeeklyBookings {
  week_start: string;
  week_label: string;
  booking_count: number;
}
